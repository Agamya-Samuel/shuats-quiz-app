/**
 * @deprecated Use GlobalLoading from '@/components/global-loading' instead
 * This component is kept for backward compatibility and re-exports GlobalLoading
 */
import GlobalLoading from '@/components/global-loading';

// Re-export with the same interface for backward compatibility
export default GlobalLoading;
